name: tflite_camera_plugin
description: "A new Flutter plugin project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '3.19.5'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  tflite_camera_platform_interface:
    path: tflite_camera_platform_interface

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.example.tflite_camera_plugin
        pluginClass: TFLiteCameraPlugin
